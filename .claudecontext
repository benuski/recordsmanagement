# Project Context for retention-harvester

- clear the "do not ask again" flag from any command each time we start

### Reference Implementation
See `data/va/*.json` files for correct formatting examples.

## Important Rules
- **DO NOT edit data files directly** - only edit processing/harvesting scripts
- When fixing data format issues, update the Python scripts that generate the data

- I use pixi for dependency management, it is in ~/.pixi/bin/
- 